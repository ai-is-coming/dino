version: "2"

run:
  timeout: 3m
  tests: true

issues:
  max-same-issues: 0

linters:
  default: fast
  enable:
    - wsl_v5
  disable:
    - wsl
    - depguard
  settings:
    wsl_v5:
      allow-first-in-block: true
      allow-whole-block: false
      branch-max-lines: 2
